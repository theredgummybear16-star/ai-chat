name: Private VPS Desktop GUI

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  vps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Maximize Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"

      - name: Start Browser-Ready Desktop
        run: |
          docker compose up -d
          echo "Waiting for GUI to be ready (approx 20 seconds)..."
          sleep 20
          docker ps

      - name: Setup Ngrok
        run: |
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok

      - name: Expose and Connect
        run: |
          echo "=========================================================="
          echo "🚀 YOUR CLOUD PC IS READY!"
          echo "URL: https://${{ secrets.NGROK_DOMAIN }}"
          echo "=========================================================="
          
          # This image serves the GUI on port 80
          ngrok http 80 --authtoken ${{ secrets.NGROK_AUTHTOKEN }} --domain ${{ secrets.NGROK_DOMAIN }}
